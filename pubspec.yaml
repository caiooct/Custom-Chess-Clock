name: custom_chess_clock
description: A new Flutter project for a customizable chess timer.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  isar: ^3.0.5
  isar_flutter_libs: ^3.0.5
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  test: ^1.22.0
  fake_async: ^1.3.1
  isar_generator: ^3.0.5
  build_runner: ^2.3.3
  mocktail: ^0.3.0

flutter:

  uses-material-design: true
